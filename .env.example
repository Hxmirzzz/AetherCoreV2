# Ruta base de facturación (ajusta esta ruta local en tu entorno)
FACTURACION_BASE_PATH=C:/Facturacion

# Carpeta de entrada y salida (se crean con base en la ruta base)
INPUT_FOLDER=${FACTURACION_BASE_PATH}/Entrada
OUTPUT_FOLDER=${FACTURACION_BASE_PATH}/Salida

# Nivel de logs (puede ser: DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=DEBUG

# Nombre fijo de la transportadora
TRANSPORTADORA=TRANSPORT_NAME